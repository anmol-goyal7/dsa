#include <iostream>
int main() {
  int largest{0};
  int arr[] = {3, 2, 5, 1, 5, 6, 9} for (int i = 0;
                                         i < sizeof(arr) / sizeof(arr[1]);
                                         i++) if (arr[i] > largest) largest =
      arr[i];
  std::cout << largest;
}
